OASIS RACE CONTROL — SIGNED SAFETY RECORDER CANDIDATE

STOP: This package is not authorized for use at Oasis merely because it is signed.

Before venue use, SAFETY-REPORT.md must show two passing Windows 11 VM rehearsals,
the exact SHA-256 in SHA256SUMS.txt, a clean Defender scan, no SmartScreen bypass,
and project-owner safety sign-off. Every box must be complete with no waiver.

The only approved commands are:
  OasisSpike.exe --mode canary
  OasisSpike.exe --mode full
  OasisSpike.exe --version
  OasisSpike.exe --help

Run from the controlled USB as a normal, non-administrator user. Do not copy the
program to the venue PC. Do not change iRacing or Windows settings. The canary
must pass before --mode full is authorized.
