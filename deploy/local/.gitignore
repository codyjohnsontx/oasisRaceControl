# Generated by `oasis-kind.sh secrets`: the local cluster's SESSION_SECRET and
# development database password. Regenerated on demand and never needed
# anywhere else - if it is lost, delete the cluster and run `up` again.
cluster.env
